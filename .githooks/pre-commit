#!/bin/bash
# blocks commits with lint/type errors - see README "Quality gate"
set -e

if [ -f .venv/bin/activate ]; then
    source .venv/bin/activate
fi

ruff format --check core/ ingest.py tests/
ruff check core/ ingest.py tests/
basedpyright core/ ingest.py tests/
