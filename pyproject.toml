[tool.ruff]
line-length = 60

[tool.ruff.lint]
extend-select = ["ANN2"]

[tool.ruff.format]
docstring-code-format = true

[tool.basedpyright]
typeCheckingMode = "standard"
exclude = ["vendor", ".venv"]
